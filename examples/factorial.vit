func main ()
{
    var n = 5;

    var res = call factorial (n);

    return 0;
}

func factorial (n)
{
    if (n == 1)
    {
        return 1;
    }

    var res = call factorial (n - 1);

    return n * res;
}
